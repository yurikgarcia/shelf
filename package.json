{
  "name": "shelf-app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "install": "concurrently \"cd client && npm install\" \"cd server && npm install\"",
    "start": "concurrently --kill-others \"cd client && npm run build\" \"cd server && npm start\""
  },
  "dependencies": {
    "@mui/x-data-grid-generator": "^5.10.0",
    "@mui/x-data-grid-pro": "^5.10.0",
    "concurrently": "^7.6.0",
    "jsonwebtoken": "^8.5.1",
    "webfontloader": "^1.6.28"
  },
  "engines": {
    "node": ">=14.0.0"
   }
}
